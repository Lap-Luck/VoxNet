<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

        <Button
            android:id="@+id/server_connect"
            android:layout_width="178dp"
            android:layout_height="50dp"
            android:text="connect"
            android:visibility="gone"
            android:textSize="25dp"
            app:layout_constraintStart_toStartOf="@+id/server_disconnect"
            app:layout_constraintTop_toTopOf="parent"></Button>

        <Button
            android:id="@+id/server_disconnect"
            android:layout_width="173dp"
            android:layout_height="57dp"
            android:text="Log off"
            android:textSize="25dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/server_connect"></Button>


    <Button
        android:id="@+id/show_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/server_disconnect" />


    <Button
        android:id="@+id/show_register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:text="Register"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/show_login" />

    <Button
        android:id="@+id/show_files"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Join Server"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/show_register" />

</androidx.constraintlayout.widget.ConstraintLayout>